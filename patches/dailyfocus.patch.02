heStore(context: Context) {
     private val prefs = context.applicationContext.getSharedPreferences(PREFS, Context.MODE_PRIVATE)
@@ -14,12 +15,28 @@
             .commit()
     }
 
-    fun read(): UsageSnapshot = UsageSnapshot(
-        firefoxMs = prefs.getLong(KEY_FIREFOX, 0L).coerceAtLeast(0L),
-        chatGptMs = prefs.getLong(KEY_CHATGPT, 0L).coerceAtLeast(0L),
-        updatedAt = prefs.getLong(KEY_UPDATED, 0L).coerceAtLeast(0L),
-        hasPermission = prefs.getBoolean(KEY_PERMISSION, false)
-    )
+    fun read(now: Long = System.currentTimeMillis()): UsageSnapshot {
+        val updatedAt = prefs.getLong(KEY_UPDATED, 0L).coerceAtLeast(0L)
+        val hasPermission = prefs.getBoolean(KEY_PERMISSION, false)
+        if (updatedAt < startOfToday(now) || updatedAt > now) {
+            return UsageSnapshot(0L, 0L, 0L, hasPermission)
+        }
+        return UsageSnapshot(
+            firefoxMs = prefs.getLong(KEY_FIREFOX, 0L).coerceAtLeast(0L),
+            chatGptMs = prefs.getLong(KEY_CHATGPT, 0L).coerceAtLeast(0L),
+            updatedAt = updatedAt,
+            hasPermission = hasPermission
+        )
+    }
+
+    private fun startOfToday(now: Long): Long = Calendar.getInstance().run {
+        timeInMillis = now
+        set(Calendar.HOUR_OF_DAY, 0)
+        set(Calendar.MINUTE, 0)
+        set(Calendar.SECOND, 0)
+        set(Calendar.MILLISECOND, 0)
+        timeInMillis
+    }
 
     private companion object {
         const val PREFS = "daily_focus_usage_cache"
diff -ruN '--exclude=.gradle' '--exclude=build' df_old/DailyFocus/app/src/main/java/com/foto6/dailyfocus/widget/DailyFocusWidgetProvider.kt DailyFocus/app/src/main/java/com/foto6/dailyfocus/widget/DailyFocusWidgetProvider.kt
--- df_old/DailyFocus/app/src/main/java/com/foto6/dailyfocus/widget/DailyFocusWidgetProvider.kt	2026-09-25 07:21:56.000000000 +0000
+++ DailyFocus/app/src/main/java/com/foto6/dailyfocus/widget/DailyFocusWidgetProvider.kt	2026-09-25 07:31:35.855623725 +0000
@@ -12,6 +12,7 @@
 import com.foto6.dailyfocus.MainActivity
 import com.foto6.dailyfocus.R
 import com.foto6.dailyfocus.core.FocusMath
+import com.foto6.dailyfocus.core.WidgetLayoutPolicy
 import com.foto6.dailyfocus.data.SettingsStore
 import com.foto6.dailyfocus.data.TaskItem
 import com.foto6.dailyfocus.data.TaskStore
@@ -104,24 +105,24 @@
             val tasks = TaskStore(context).getTasks()
             val views = RemoteViews(context.packageName, R.layout.widget_daily_focus)
 
-            val limit = settings.limitMinutes
+            val goal = settings.goalMinutes
             val total = snapshot.totalMs
             views.setTextViewText(
                 R.id.widget_usage,
-                "${FocusMath.formatDuration(total)} / ${FocusMath.formatLimit(limit)}"
+                "${FocusMath.formatDuration(total)} / ${FocusMath.formatGoal(goal)}"
             )
             views.setProgressBar(
                 R.id.widget_progress,
                 1000,
-                FocusMath.progressPermille(total, limit),
+                FocusMath.progressPermille(total, goal),
                 false
             )
 
             val status = when {
                 !snapshot.hasPermission -> "Нужен доступ к статистике"
-                FocusMath.overLimitMs(total, limit) > 0L ->
-                    "+${FocusMath.formatDuration(FocusMath.overLimitMs(total, limit))} сверх лимита"
-                else -> "Осталось ${FocusMath.formatDuration(FocusMath.remainingMs(total, limit))}"
+                FocusMath.overGoalMs(total, goal) > 0L ->
+                    "Цель выполнена · +${FocusMath.formatDuration(FocusMath.overGoalMs(total, goal))}"
+                else -> "Осталось ${FocusMath.formatDuration(FocusMath.remainingMs(total, goal))}"
             }
             views.setTextViewText(R.id.widget_status, status)
             views.setTextViewText(
@@ -131,18 +132,13 @@
 
             val widgetHeight = manager.getAppWidgetOptions(appWidgetId)
                 .getInt(AppWidgetManager.OPTION_APPWIDGET_MIN_HEIGHT, 280)
-            val (showStatus, showApps, maxTasks) = when {
-                widgetHeight < 230 -> Triple(false, false, 1)
-                widgetHeight < 285 -> Triple(true, false, 2)
-                widgetHeight < 320 -> Triple(true, true, 3)
-                else -> Triple(true, true, 4)
-            }
-            views.setViewVisibility(R.id.widget_status, if (showStatus) View.VISIBLE else View.GONE)
-            views.setViewVisibility(R.id.widget_apps, if (showApps) View.VISIBLE else View.GONE)
+            val layout = WidgetLayoutPolicy.forHeight(widgetHeight)
+            views.setViewVisibility(R.id.widget_status, if (layout.showStatus) View.VISIBLE else View.GONE)
+            views.setViewVisibility(R.id.widget_apps, if (layout.showApps) View.VISIBLE else View.GONE)
 
             val completed = tasks.count { it.done }
             views.setTextViewText(R.id.widget_tasks_count, "$completed/${tasks.size}")
-            bindTasks(context, views, tasks.take(maxTasks))
+            bindTasks(context, views, tasks.take(layout.maxTasks))
 
             views.setOnClickPendingIntent(R.id.widget_root, openAppIntent(context))
             views.setOnClickPendingIntent(R.id.widget_refresh, refreshIntent(context))
diff -ruN '--exclude=.gradle' '--exclude=build' df_old/DailyFocus/app/src/main/res/values/strings.xml DailyFocus/app/src/main/res/values/strings.xml
--- df_old/DailyFocus/app/src/main/res/values/strings.xml	2026-09-25 07:06:59.000000000 +0000
+++ DailyFocus/app/src/main/res/values/strings.xml	2026-09-25 07:31:35.855868434 +0000
@@ -1,5 +1,5 @@
 <?xml version="1.0" encoding="utf-8"?>
 <resources>
     <string name="app_name">Daily Focus</string>
-    <string name="widget_description">Время Firefox + ChatGPT и задачи на сегодня</string>
+    <string name="widget_description">Дневная цель Firefox + ChatGPT и задачи на сегодня</string>
 </resources>
diff -ruN '--exclude=.gradle' '--exclude=build' df_old/DailyFocus/app/src/test/java/com/foto6/dailyfocus/core/FocusMathTest.kt DailyFocus/app/src/test/java/com/foto6/dailyfocus/core/FocusMathTest.kt
--- df_old/DailyFocus/app/src/test/java/com/foto6/dailyfocus/core/FocusMathTest.kt	2026-09-25 07:13:01.000000000 +0000
+++ DailyFocus/app/src/test/java/com/foto6/dailyfocus/core/FocusMathTest.kt	2026-09-25 07:31:35.855697476 +0000
@@ -1,7 +1,9 @@
 package com.foto6.dailyfocus.core
 
 import org.junit.Assert.assertEquals
+import org.junit.Assert.assertTrue
 import org.junit.Test
+import kotlin.random.Random
 
 class FocusMathTest {
     @Test fun zeroDuration() = assertEquals("0м", FocusMath.formatDuration(0))
@@ -9,10 +11,32 @@
     @Test fun exactMinute() = assertEquals("1м", FocusMath.formatDuration(60_000))
     @Test fun mixedHoursAndMinutes() = assertEquals("2ч 17м", FocusMath.formatDuration((2 * 60 + 17) * 60_000L))
     @Test fun exactHour() = assertEquals("2ч", FocusMath.formatDuration(120 * 60_000L))
+    @Test fun negativeDurationClampsToZero() = assertEquals("0м", FocusMath.formatDuration(-1))
     @Test fun progressCapsAtOneHundredPercent() = assertEquals(1000, FocusMath.progressPermille(200 * 60_000L, 120))
     @Test fun progressHalf() = assertEquals(500, FocusMath.progressPermille(60 * 60_000L, 120))
-    @Test fun progressHandlesInvalidLimit() = assertEquals(0, FocusMath.progressPermille(60_000L, 0))
-    @Test fun overLimitOnlyReturnsExcess() = assertEquals(17 * 60_000L, FocusMath.overLimitMs(137 * 60_000L, 120))
+    @Test fun progressHandlesInvalidGoal() = assertEquals(0, FocusMath.progressPermille(60_000L, 0))
+    @Test fun overGoalOnlyReturnsExcess() = assertEquals(17 * 60_000L, FocusMath.overGoalMs(137 * 60_000L, 120))
     @Test fun remainingNeverNegative() = assertEquals(0L, FocusMath.remainingMs(137 * 60_000L, 120))
     @Test fun negativeUsageDoesNotIncreaseRemaining() = assertEquals(120 * 60_000L, FocusMath.remainingMs(-60_000L, 120))
+
+    @Test fun stressMathInvariants() {
+        val random = Rand